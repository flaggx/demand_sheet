# Deprecated: desktop app (Python / Tkinter)

This folder holds the previous **Windows desktop** Route Viewer (`route_viewer.py`, `RouteViewer.bat`, etc.). It is no longer the active application; development continues in the **Next.js** app at the repository root.

To run the old app locally, use the files here with Python 3.8+ as described in `SETUP_WINDOWS.md` in this folder.
